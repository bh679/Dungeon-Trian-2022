using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class ReceiveButtonPressEvent : MonoBehaviour
{

	public enum ButtonType
	{
		GripLeft = 0,
		TriggerLeft = 1,
		GripRight = 2,
		TriggerRight = 3,
		GripBoth = 4,
		TriggerBoth = 5,
		A = 6,
		B = 7,
		X = 8,
		Y = 9,
		LeftJoyStick = 10,
		RightJoyStick = 11,
		HomeLeft = 12,
		HomeRight = 13,
		HomeBoth = 12
		
	}

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
