using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using BrennanHatton.DungeonTrain.Rooms;

public class AssignTerrainToTrain : MonoBehaviour
{
	public RoomCreator Train;
	
    // Start is called before the first frame update
    void Start()
	{
		/*if(Train.leftTerrain == null)
			Train.leftTerrain = this.transform;
		else
		Train.rightTerrain = this.transform;*/
    }

}
